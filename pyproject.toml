[project]
name = "fly-in"
version = "0.1.0"
description = "Drone traffic simulation"
requires-python = ">=3.12,<3.13"
dependencies = [
    "rich>=14.0.0",
    "textual>=0.50.0",
]

[dependency-groups]
dev = [
    "flake8>=7.0.0",
    "mypy>=1.0.0",
]